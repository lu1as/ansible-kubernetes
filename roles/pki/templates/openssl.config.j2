[req]
prompt = no
default_md = sha256
req_extensions = v3_req
x509_extensions = v3_req
distinguished_name = k8s

[k8s]
C = {{pki_country}}
ST = {{pki_state}}
L = {{pki_city}}
O = {{pki_organization}}
OU = kubernetes
CN = {{hostvars[item].ansible_hostname}}.{{kube_tld}}

[v3_req]
subjectAltName = @alt_names

[alt_names]
DNS.1 = {{hostvars[item].ansible_hostname}}.{{kube_tld}}
{% if item in groups['kube_masters'] %}
DNS.2 = master.{{kube_tld}}
DNS.3 = kubernetes.default.srv.{{kube_dns_domain}}
DNS.4 = kubernetes.default.srv
DNS.5 = kubernetes.default
DNS.6 = kubernetes
{% endif %}
